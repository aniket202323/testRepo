CREATE FUNCTION [dbo].[fnLocal_PG_Cmn_GetLineStatus]
(@PU_Id INT NULL, @SpecificTime DATETIME NULL, @UseLocal BIT NULL)
RETURNS VARCHAR (25)
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN NULL
END

