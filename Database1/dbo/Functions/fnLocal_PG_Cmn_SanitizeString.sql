CREATE FUNCTION [dbo].[fnLocal_PG_Cmn_SanitizeString]
(@p_StringToSanitize VARCHAR (MAX) NULL)
RETURNS VARCHAR (MAX)
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN NULL
END

