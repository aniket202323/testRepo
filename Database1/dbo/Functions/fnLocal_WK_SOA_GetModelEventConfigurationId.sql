CREATE FUNCTION [dbo].[fnLocal_WK_SOA_GetModelEventConfigurationId]
(@ModelNum VARCHAR (10) NULL, @EventType VARCHAR (25) NULL, @PUId INT NULL)
RETURNS VARCHAR (10)
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN NULL
END

