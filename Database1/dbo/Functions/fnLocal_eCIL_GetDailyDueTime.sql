CREATE FUNCTION [dbo].[fnLocal_eCIL_GetDailyDueTime]
(@SpecificTime DATETIME NULL, @TestTime VARCHAR (5) NULL)
RETURNS DATETIME
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN NULL
END

