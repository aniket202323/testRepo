CREATE FUNCTION [dbo].[fnLocal_eCIL_GetModules]
(@IDsList VARCHAR (8000) NULL, @IDsType VARCHAR (10) NULL)
RETURNS 
    @GeneratedTableName TABLE (
        [SlaveUnitId] INT NULL)
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END

