CREATE FUNCTION [dbo].[fnLocal_eCIL_GetNbrStops]
(@PlantModelLevel VARCHAR (50) NULL, @KeyId INT NULL, @StartTime DATETIME NULL, @EndTime DATETIME NULL, @RouteIDs VARCHAR (8000) NULL, @TeamIDs VARCHAR (8000) NULL)
RETURNS VARCHAR (50)
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN NULL
END

