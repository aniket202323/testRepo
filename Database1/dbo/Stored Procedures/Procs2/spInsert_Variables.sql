CREATE PROCEDURE [dbo].[spInsert_Variables]
@VarId VARCHAR (MAX) NULL, @VarDesc VARCHAR (MAX) NULL, @PUDesc VARCHAR (MAX) NULL, @PLDesc VARCHAR (MAX) NULL, @ProdDesc VARCHAR (MAX) NULL, @Freq INT NULL, @AuditFreq VARCHAR (MAX) NULL, @TestTime VARCHAR (MAX) NULL
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END


