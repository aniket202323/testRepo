CREATE PROCEDURE [dbo].[Splocal_CmnPEmsgCreateMessage]
@msg VARCHAR (1000) NULL, @StartTime VARCHAR (50) NULL, @EndTime VARCHAR (50) NULL, @userName VARCHAR (100) NULL, @UdeId INT NULL OUTPUT
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END


