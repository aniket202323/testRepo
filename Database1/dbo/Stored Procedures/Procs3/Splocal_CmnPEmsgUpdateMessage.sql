CREATE PROCEDURE [dbo].[Splocal_CmnPEmsgUpdateMessage]
@UdeId INT NULL OUTPUT, @msg VARCHAR (1000) NULL, @StartTime VARCHAR (50) NULL, @EndTime VARCHAR (50) NULL, @Ack BIT NULL, @userName VARCHAR (100) NULL
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END


