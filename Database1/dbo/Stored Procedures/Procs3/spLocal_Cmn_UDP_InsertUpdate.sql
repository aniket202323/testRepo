CREATE PROCEDURE [dbo].[spLocal_Cmn_UDP_InsertUpdate]
@TableValues VARCHAR (MAX) NULL, @TableTypes VARCHAR (MAX) NULL, @TableNames VARCHAR (MAX) NULL, @User_Id INT NULL
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END


