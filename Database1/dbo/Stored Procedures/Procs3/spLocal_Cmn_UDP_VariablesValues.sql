CREATE PROCEDURE [dbo].[spLocal_Cmn_UDP_VariablesValues]
@LinesIDs VARCHAR (MAX) NULL, @TableValues VARCHAR (MAX) NULL, @TableNames VARCHAR (MAX) NULL
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END


