CREATE PROCEDURE [dbo].[spLocal_eCIL_SaveTask]
@TestId BIGINT NULL, @CurrentDueTime DATETIME NULL, @CurrentResult VARCHAR (50) NULL, @UserId INT NULL, @CommentText VARCHAR (500) NULL, @IsInShift BIT NULL OUTPUT, @ErrorMessage VARCHAR (1000) NULL OUTPUT
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END


