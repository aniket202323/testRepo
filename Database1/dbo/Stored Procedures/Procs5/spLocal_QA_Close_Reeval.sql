CREATE PROCEDURE [dbo].[spLocal_QA_Close_Reeval]
@OutputValue VARCHAR (60) NULL OUTPUT, @Status_varid INT NULL, @Disposition VARCHAR (30) NULL, @timestamp DATETIME NULL, @ei_alarm VARCHAR (30) NULL
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END


