CREATE PROCEDURE [dbo].[spLocal_SetShiftValue]
@outputvalue VARCHAR (25) NULL OUTPUT, @EventEndTime VARCHAR (25) NULL, @PUID INT NULL
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END


