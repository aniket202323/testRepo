CREATE PROCEDURE [dbo].[spLocal_WFTM_DeleteAddTeams]
@ReturnStatus INT NULL OUTPUT, @ReturnMessage VARCHAR (800) NULL OUTPUT, @SpUseNumber INT NULL, @TeamNameToDelete VARCHAR (10) NULL, @TeamNames VARCHAR (50) NULL
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END


