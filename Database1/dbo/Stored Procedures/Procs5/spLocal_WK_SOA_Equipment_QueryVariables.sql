CREATE PROCEDURE [dbo].[spLocal_WK_SOA_Equipment_QueryVariables]
@LineMask VARCHAR (50) NULL, @UnitMask VARCHAR (50) NULL, @VarMask VARCHAR (50) NULL, @UserId INT NULL, @DeptMask VARCHAR (50) NULL
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END


