CREATE PROCEDURE [dbo].[spLocal_WK_SOA_Specifications_GetCentralSpecifications]
@ProductPropertyListOrMask VARCHAR (MAX) NULL, @SpecificationListOrMask VARCHAR (MAX) NULL, @CharacteristicListOrMask VARCHAR (MAX) NULL, @PageNumber INT NULL, @PageSize INT NULL
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END


