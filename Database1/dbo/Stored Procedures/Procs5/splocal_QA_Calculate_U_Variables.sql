CREATE PROCEDURE [dbo].[splocal_QA_Calculate_U_Variables]
@Output FLOAT (53) NULL OUTPUT, @NbrOfSamples VARCHAR (30) NULL, @M FLOAT (53) NULL, @A FLOAT (53) NULL
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END


