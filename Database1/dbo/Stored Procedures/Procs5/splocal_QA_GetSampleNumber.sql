CREATE PROCEDURE [dbo].[splocal_QA_GetSampleNumber]
@strLineType VARCHAR (30) NULL OUTPUT, @strAttType VARCHAR (30) NULL OUTPUT, @strNumberSample VARCHAR (10) NULL OUTPUT, @varid INT NULL, @puid INT NULL, @calc VARCHAR (30) NULL
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END


