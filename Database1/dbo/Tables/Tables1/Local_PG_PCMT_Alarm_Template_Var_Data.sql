CREATE TABLE [dbo].[Local_PG_PCMT_Alarm_Template_Var_Data] (
    [pu_id]     INT      NOT NULL,
    [at_id]     INT      NOT NULL,
    [var_id]    INT      NOT NULL,
    [timestamp] DATETIME NOT NULL
);

