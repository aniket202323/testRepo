CREATE TABLE [dbo].[Local_PG_PCMT_Sheet_Variables] (
    [pu_id]     INT      NOT NULL,
    [sheet_id]  INT      NOT NULL,
    [var_id]    INT      NOT NULL,
    [var_order] INT      NOT NULL,
    [timestamp] DATETIME NOT NULL
);

