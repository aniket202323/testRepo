CREATE TABLE [dbo].[ScannerMode] (
    [Id]           INT            NOT NULL,
    [ModeMenu]     NVARCHAR (150) NULL,
    [MenuSequence] INT            NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

